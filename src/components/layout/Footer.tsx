export function Footer() {
  return (
    <footer className="mt-auto border-t border-line">
      <div className="mx-auto flex w-full max-w-7xl flex-col gap-2 px-4 py-8 text-sm text-muted sm:flex-row sm:items-center sm:justify-between sm:px-6 lg:px-8">
        <p>MovieMind — Phase 1 foundation. Local UI placeholders only.</p>
        <p>Live catalog, accounts, and recommendations come later.</p>
      </div>
    </footer>
  );
}
